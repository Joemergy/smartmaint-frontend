# Example environment variables for production
# Backend API base URL (set this to your deployed backend URL)
VITE_API_URL=https://api.smartmaint.example.com
# For Create React App compatibility (if using CRA)
REACT_APP_API_URL=https://api.smartmaint.example.com
